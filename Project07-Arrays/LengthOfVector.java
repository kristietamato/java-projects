// Created by Kristie Nguyen on 10/14/2014.
// LengthOfVector - Finds the length of a vector.
// The length of a vector is the square root of 
// the sum of the squares of the elements in the array.

public class LengthOfVector 
{
	public void main( String[] args )
	{
	public VectorLength()
	{
		double length = (double[] elements)
		{
			double sum = 0;
		
			for ( int count = 0; count < elements.length; count++)
			{
				sum = sum + elements[ count ] * elements[ count ];
			}
		
			return Math.sqrt( sum );
		}
	}
}
}
